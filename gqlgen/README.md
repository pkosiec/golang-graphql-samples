# GraphQL API sample using GQLGen
Simple app written in Go using [GQLGen](https://github.com/vektah/gqlgen) library.

## Generate schema
To generate schema, simply run:
```bash
go generate ./...
```

## Run
Run app by executing:
```bash
go run main.go
```